# Concept art

Source reference images for the ship rosters, buildings, and other assets
described in `../WORLD_DESIGN.md`. Add image files here with descriptive
names (e.g. `ship_roster_sketch_01.jpg`) and cross-reference them from the
relevant section of `WORLD_DESIGN.md`.

## Ship roster sketch (pending — photo still needs saving here)

A hand-drawn concept sketch of the full ship roster (both factions'
silhouettes, propulsion trails, and weapon/impact VFX notes) was reviewed
twice now — the details are transcribed into `WORLD_DESIGN.md`'s Section
11 ("Ships") and Section 17 ("Visual & asset reference", specifically the
"Ships" and "Propulsion, weapons & VFX identity" subsections).

The source photo itself still needs to be saved into this folder — Claude
can view images pasted inline in chat but has no way to write that image
data to disk directly, so this is a manual step:

1. Save the notebook-page photo as a file (e.g. `ship_roster_sketch_01.jpg`)
   directly into this `concept_art/` folder.
2. Once it's here, the cross-references in Section 17 will resolve to an
   actual file instead of "pending."

One open question spotted on the sketch: a small-hull icon is separately
labeled **"S-ship upgraded,"** distinct from the plain baseline Small ship
icon next to it. This is flagged in Section 17 as needing clarification —
it's not yet clear if that's a visual-only upgrade to the baseline Small
hull or an early name for one of the specialist ships (Shroud is drawn
immediately next to it in the sketch).
